version: "3.9"

services:
  mariadb:
    image: mariadb:11.4
    container_name: riforma-mariadb
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: example-root-password
      MARIADB_DATABASE: riforma
      MARIADB_USER: riforma
      MARIADB_PASSWORD: riforma
    ports:
      - "3307:3306"
    volumes:
      - mariadb-data:/var/lib/mysql
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --lower_case_table_names=1
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-hmariadb", "-priforma"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  mariadb-exporter:
    image: prom/mysqld-exporter:v0.15.0
    container_name: riforma-mariadb-exporter
    restart: unless-stopped
    environment:
      DATA_SOURCE_NAME: "riforma:riforma@(mariadb:3306)/"
    depends_on:
      mariadb:
        condition: service_healthy
    ports:
      - "9104:9104"

volumes:
  mariadb-data:
